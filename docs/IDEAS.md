- have a raw view of html, md (basic rendering) and use our template
- 
With that, all pre-implementation design work is done. The remaining pre-M0 items are the two spikes (Fumadocs restyling fit, hypothes.is anchoring port) and the name/domain decision. Want me to kick off M0 — monorepo scaffold with the Laravel recipe + Fumadocs shell styled to DESIGN.md (which doubles as the Fumadocs spike)?
- add an agent that syncs documents to server
- 
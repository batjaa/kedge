Mixed inline and block constructs

Text with bold, emphasis, strikethrough, and a hard break here:
first line
second line.

A blockquote quoting the spec.
It spans two source lines but one logical paragraph.

Text after a thematic break. The rule itself contributes no anchor text, but the
paragraphs on either side keep their own offsets.
# Hello-World

This is the raw file body the GitHub contents API returns under the
`application/vnd.github.raw` media type — the bytes of the file itself, not a
base64-wrapped JSON envelope.

## Usage

```php
echo 'kedge';
```

A second paragraph so the projection (#18) has something to chew on later.

# Private RFC 042: Widget Protocol

Status: draft · Visibility: private repo

This file stands in for a document fetched from a **private** GitHub repository
through an authenticated PAT request. Its bytes are unremarkable — the point of
the fixture is that the connector reached it at all, with the token on the wire.

## Summary

The widget protocol negotiates a frobnicator handshake before any payload flows.
